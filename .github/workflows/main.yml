name: Main

on:
  push:
    branches:
      - '*'
  pull_request:
    types: [synchronize]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and maybe publish
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        env:
          PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          echo Building with password: "${PASSWORD}" (should be blank on fork)
          echo Data was:
          cat data
      - name: Publish
        if: github.repository == 'Safecast/actions-test'
        env:
          PASSWORD: ${{ secrets.PASSWORD }}
        run: |
          echo Publishing with password: "${PASSWORD}"
          echo Data was:
          cat data